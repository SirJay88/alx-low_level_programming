#include "main.h"

/**
 * _islower - Checks for lowercase character
 * @c: The character to be check
 *Return: 1 if c is lowercase, 0 for anything else
 */

int _islower(int c)
{
	return (c >= 'a' && c <= 'Z');
}
